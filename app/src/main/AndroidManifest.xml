<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tr.yildiz.edu.sermedkerim">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidProgramming">
        <activity android:name=".ShareQuiz"></activity>
        <activity android:name=".UpdateScreen" />
        <activity android:name=".CreateQuiz" />
        <activity android:name=".QuizSettings" />
        <activity android:name=".QuestionListActivity" />
        <activity android:name=".AddQuestionScreen" />
        <activity android:name=".ShowScreen" />
        <activity android:name=".SignUpScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>